package com.digisafe.core.model

// User requested 'empty data class', but data classes must have at least one primary constructor parameter.
// Using a standard class to avoid compilation errors.
class CallMetadata {
}
